{
  "name": "@postilla/server",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./dist/main.js",
  "//exports": "Subpaths resolve to source, not dist: the migration tool binds to the same definitions the server uses, with no build step between them. Note that `exports` may not mix subpath keys with anything else, so this note lives outside it.",
  "exports": {
    ".": "./dist/main.js",
    "./schema": "./src/infrastructure/db/schema.ts",
    "./domain/page-path": "./src/domain/page/path.ts"
  },
  "scripts": {
    "dev": "tsx watch src/main.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/main.js",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "tsx src/infrastructure/db/migrate.ts",
    "db:drift": "drizzle-kit check",
    "db:studio": "drizzle-kit studio"
  },
  "dependencies": {
    "@fastify/cors": "^11.3.0",
    "@fastify/helmet": "^13.1.0",
    "@fastify/rate-limit": "^11.2.0",
    "@fastify/static": "^10.1.2",
    "@fastify/swagger": "^9.8.1",
    "@postilla/contract": "workspace:*",
    "drizzle-orm": "^0.45.2",
    "fastify": "^5.11.0",
    "fastify-type-provider-zod": "^7.0.0",
    "pg": "^8.22.0",
    "pino": "^10.3.1",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/pg": "^8.20.2",
    "drizzle-kit": "^0.31.10",
    "tsx": "^4.23.1",
    "typescript": "5.9.3"
  }
}
